# Walkthrough recordings

Screen recordings and screenshots of the Semantic Sheets MVP, kept on a media-only branch so the code history stays small. Open any `.mp4` in the GitHub file viewer to play it.
